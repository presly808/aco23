package projectzero.utils;

import projectzero.model.Order;
import projectzero.model.User;

import java.util.List;

public class JSONUtilsTest {

    private static User user;
    private static Order order;

    /*
    @Before
    public void setUp() {
        user = new User("Les@gmail.com", "pass");
        order = new Order(0, "Title", "descr", 100, "Good", user);
    }

    @Test
    public void readFromFile() throws Exception {
        List<User> userList = JSONUtils.readFromFile(userJsonRead, User[].class);
    }

    @Test
    public void writeListIntoFile() {
        JSONUtils.writeListIntoFile(userJsonWrite, Arrays.asList(user));
        JSONUtils.writeListIntoFile(orderJsonWrite, Arrays.asList(order));
    }
    */

}